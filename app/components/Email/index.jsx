import React from 'react';

import TextField from '@material-ui/core/TextField';

const Email = () => (
  <TextField
    fullWidth
    label="Email Address"
    margin="normal"
  />
);

export default Email;
