import React from 'react';

import TextField from '@material-ui/core/TextField';

const FirstName = () => (
  <TextField
    fullWidth
    label="First name"
    margin="normal"
  />
);

export default FirstName;
